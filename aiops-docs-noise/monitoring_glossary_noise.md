# 监控术语词典（噪声文档）

> 本文档是术语罗列页，不含任何排查步骤。检索系统若对"CPU""磁盘""内存"等词只做词面匹配，会把本文档误判为相关——它是评测"噪声/易混淆查询"能力的干扰项。真正的排障内容见 cpu_high_usage.md 等正式手册。

## 术语表

- CPU：中央处理器。相关词：使用率、负载、us/sy/wa。
- 磁盘：存储设备。相关词：写入、IO、水位。
- 内存：运行时存储。相关词：OOM、GC、堆。
- P99：第 99 百分位延迟。
- 错误率：失败请求占比。

（每个词都提到，但每个主题都只有一句定义。）
